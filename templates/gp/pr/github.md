# GitHub Pull Request Body Template

Use this template when creating PRs via `gh pr create`. Replace all `<PLACEHOLDER>` values.

---

## Summary

Automated test suite generated for **[<TICKET_ID>](<TICKET_URL>)**: <TICKET_TITLE>

<FEATURE_DOC>

---

## Test Results

| Metric | Value |
|--------|-------|
| **Framework** | <FRAMEWORK> |
| **Total Tests** | <TOTAL> |
| ✅ **Passed** | <PASSED> |
| ❌ **Failed** | 0 |
| ⏱ **Duration** | <DURATION>s |
| **Branch** | `<BRANCH>` |

---

## What's Tested

<SCENARIO_LIST formatted as checklist>
- [ ] <Scenario 1>
- [ ] <Scenario 2>
- [ ] <Edge Case 1>

---

## Architecture

This PR follows the **Page Object Model** pattern:

```
tests/<feature>.spec.js         ← Test file (calls POM methods)
pages/<Feature>Page.js          ← Page Object (encapsulates UI interactions)
helpers/<feature>Helper.js      ← Reusable helper functions
config/selectors/<feature>.json ← Element selectors (XPath/CSS)
config/environments.json        ← Environment config (template, no credentials)
```

---

## Files Changed

| File | Change |
|------|--------|
| `tests/<feature>.spec.<ext>` | New — <N> test cases |
| `pages/<Feature>Page.<ext>` | New — POM class |
| `config/selectors/<feature>.json` | New — <N> selectors |
| `helpers/<feature>Helper.<ext>` | New — helper functions |

---

## Test Reports

- [📊 Allure Report](<ALLURE_REPORT_PATH>)
- [📄 HTML Report](<HTML_REPORT_PATH>)

---

## Checklist

- [x] Tests follow POM pattern
- [x] No hardcoded credentials or URLs
- [x] All `expect()` calls include descriptive messages
- [x] Explicit waits only (no `sleep()`)
- [x] Selectors in JSON files, not inline
- [x] All tests passing

---

> 🤖 Generated by [GP Test Agent](https://claude.ai/claude-code) — [View Run](<RUN_ID>)
> Ticket: [<TICKET_ID>](<TICKET_URL>) | Framework: <FRAMEWORK>
